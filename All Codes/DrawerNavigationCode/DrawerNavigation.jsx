import { Text, View } from "react-native";

// drawer navigation is like a side bar
// install gesture handler and re animated and drawer
const DrawerNavigation = () => {
  return (
    <View>
      <Text>DrawerNavigation</Text>
    </View>
  );
};

export default DrawerNavigation;
